name: waitwhat
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  google_generative_ai: 0.4.7
  notification_listener_service: 0.3.5
  home_widget: 0.9.1
  drift: ^2.22.0
  drift_flutter: ^0.2.4
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.22.0
  build_runner: ^2.4.0