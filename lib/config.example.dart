// Copy this file to config.dart and fill in your own values.
// config.dart is gitignored — never commit real keys.

// Groq API key — get one free at https://console.groq.com
const String kGroqApiKey = '';

// GitHub fine-grained PAT with `issues:write` scope on kGithubRepo.
// Used by the in-app feedback form to create GitHub issues.
// Leave empty to disable the feedback form.
const String kGithubToken = '';

// GitHub username or org that owns the repository.
const String kGithubOwner = '';

// Repository name where feedback issues will be created.
const String kGithubRepo = '';